#shell命令字
* 目标：
  * 高效
  * 严谨
* 善用
  * man
  * info
  * 管道
* 单个命令字
  * ls 
    * ls -l  ---> ll
  * cd
  * touch
  * rm
  * mkdir
    * mkdir -p
  * mv
  * cp
    * cp -r
    * cp -f
  * echo
  * cat
  * more/head/tail
    * head/tail -n
  * rz/sz
  * vi——博大精深
  * grep
    * grep -n
    * grep -A/B/C
    * grep -h
  * find
    * find -name . "*test.file"
  * awk
    * awk -F'aaa|bbbb|cccc|dddd' '{print $2,$3}'
    * awk -F'aaa|bbbb|cccc|dddd' '{printf "%s---%d\n",$2,$3}'
    * BEGIN END
  * sort
    * sort -n
    * sort -r
  * uniq
    * uniq -c
    * uniq -u
    * uniq -d*
* 复合语句
  * if/while/for
* 特殊
  * s3cmd

``man``
+ 官方文档
+ [](https://www.linuxidc.com/Linux/2017-03/142407.htm)

